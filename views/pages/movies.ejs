<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Movies</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap');

    /* Reset and base */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }
    body {
      font-family: 'Inter', sans-serif;
      background: #fcfcfc;
      color: #4e443c;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 30px 15px;
    }

    /* Header styling */
    header {
      width: 100%;
      max-width: 1100px;
      padding: 20px 40px;
      background: #fff;
      box-shadow: 0 5px 25px rgba(189, 164, 98, 0.2);
      border-radius: 18px;
      text-align: center;
      font-size: 28px;
      font-weight: 700;
      color: #bda62a;
      user-select: none;
      margin-bottom: 40px;
    }

    /* Page title */
    h1 {
      font-weight: 700;
      font-size: 2.8rem;
      color: #5c4f39;
      margin-bottom: 40px;
      text-align: center;
      max-width: 1100px;
      width: 100%;
      user-select: none;
    }

    /* Search bar */
    .search-bar {
      display: flex;
      justify-content: center;
      margin-bottom: 40px;
      width: 100%;
      max-width: 680px;
      border-radius: 35px;
      overflow: hidden;
      box-shadow: 0 0 22px rgba(189, 164, 98, 0.5);
      background-color: #fff;
    }

    .search-bar input[type="text"] {
      flex: 1;
      padding: 14px 18px;
      font-size: 1rem;
      border: none;
      color: #5c4f39;
      font-weight: 600;
      transition: background-color 0.3s ease;
    }
    .search-bar input[type="text"]:focus {
      outline: none;
      background-color: #faf6e1;
      box-shadow: inset 2px 0 10px #bda62a99;
      color: #443a22;
    }

    .search-bar button {
      background-image: linear-gradient(90deg, #bda62a, #a9911e);
      border: none;
      color: #fffaf0;
      font-weight: 700;
      padding: 0 28px;
      cursor: pointer;
      font-size: 1.05rem;
      border-radius: 0 35px 35px 0;
      box-shadow: 0 0 24px #bda62aaa;
      transition: background-color 0.3s ease, box-shadow 0.3s ease;
    }
    .search-bar button:hover {
      background-image: linear-gradient(90deg, #a9911e, #bda62a);
      box-shadow: 0 0 38px #d6ca48cc;
    }

    /* Movies grid */
    .movies-container {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 28px;
      max-width: 1100px;
      width: 100%;
      padding: 0 20px;
    }

    /* Movie card */
    .movie-card {
      background: #fffbea;
      border-radius: 16px;
      box-shadow: 0 8px 28px rgba(189, 164, 98, 0.3);
      overflow: hidden;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      user-select: none;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .movie-card:hover {
      transform: translateY(-6px);
      box-shadow: 0 14px 42px rgba(189, 164, 98, 0.55);
    }

    .movie-card img {
      width: 100%;
      height: 320px;
      object-fit: cover;
      border-radius: 16px 16px 0 0;
      background: #fbf8dd;
      transition: transform 0.35s ease;
    }
    .movie-card img:hover {
      transform: scale(1.05);
    }

    .movie-card h3 {
      font-size: 1.25rem;
      font-weight: 700;
      margin: 18px 0 4px 0;
      color: #796d4b;
      text-align: center;
      padding: 0 14px;
    }

    .movie-card p {
      font-size: 0.9rem;
      color: #988c6d;
      text-align: center;
      padding: 0 14px 16px 14px;
      min-height: 40px;
    }

    .movie-card a {
      margin: 0 auto 24px auto;
      padding: 12px 25px;
      background: linear-gradient(90deg, #bda62a, #a18b15);
      color: #fffaf0;
      font-weight: 700;
      text-decoration: none;
      border-radius: 30px;
      display: block;
      width: 65%;
      text-align: center;
      box-shadow: 0 0 28px #bda62a88;
      transition: background-color 0.3s ease, box-shadow 0.3s ease, transform 0.3s ease;
      user-select: none;
    }
    .movie-card a:hover {
      background: linear-gradient(90deg, #a18b15, #bda62a);
      box-shadow: 0 0 42px #d6ca48bb;
      transform: translateY(-3px);
    }

    /* Responsive */
    @media (max-width: 768px) {
      .search-bar input[type="text"] {
        width: 240px;
      }
      .movies-container {
        padding: 0 16px;
        gap: 22px;
      }
    }
    @media (max-width: 480px) {
      h1 {
        font-size: 2.4rem;
      }
      .movie-card h3 {
        font-size: 1.1rem;
      }
      .movie-card img {
        height: 260px;
      }
      .movie-card a {
        width: 85%;
        padding: 10px 18px;
        font-size: 0.95rem;
      }
    }
  </style>
</head>
<body>
  <%- include('../partials/header', { user: user }) %>

  <h1>Movies Near You</h1>

  <div class="search-bar">
    <form action="/movies" method="GET">
      <input type="text" name="search" placeholder="Search for movies..." value="<%= query || '' %>" />
      <button type="submit">Search</button>
    </form>
  </div>

  <div class="movies-container">
    <% if (movies && movies.length > 0) { %>
      <% movies.forEach(movie => { %>
        <div class="movie-card">
          <img 
            src="<%= movie.posterUrl || (movie.Poster && movie.Poster !== 'N/A' ? movie.Poster : '/images/default-poster.jpg') %>" 
            alt="<%= movie.title %>"
          >
          <h3><%= movie.title %></h3>
          <p><%= movie.description || movie.year || '' %></p>
          <% if (movie._id) { %>
            <a href="/movies/<%= movie._id %>/seats">Book Seats</a>
          <% } else { %>
            <a href="/movies/<%= movie.imdbID %>/seats">Book Seats</a>
          <% } %>
        </div>
      <% }) %>
    <% } else { %>
      <p style="text-align:center; color:#938765; font-weight:600; user-select:none;">
        No movies found. Try searching for another title.
      </p>
    <% } %>
  </div>
  <script src="/js/moviesCache.js"></script>

</body>
</html>
